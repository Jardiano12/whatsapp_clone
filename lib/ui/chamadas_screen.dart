import 'package:flutter/material.dart';

class ChamadasScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context){
    return new Container(
        child: new Center(
          child: new Text('Chamadas', style: new TextStyle(fontSize: 20.0),),
        )
    );
  }
}